extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
      h1 Timur Guo Chen's Project Portfolio
      p.lead.text-light
        | Built with Node.js and Express, the portfolio is a static web app that showcase my JavaScript and Data Science Projects.
  
  article.grid-container.portfolio-index
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each project in projects
        .cell.animate
          a(href=`/project/${project.id}`)
            img.thumbnail.zoom(src=`${project.image_urls[0]}`)
            h5= project.project_name